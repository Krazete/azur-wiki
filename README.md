# Azur Lane
## Project Identity: TB

Windows

```sh
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python -m itemchart
```

Mac

```sh
python -m venv venv
source venv\bin\activate
pip install -r requirements.txt
python -m itemchart
```
